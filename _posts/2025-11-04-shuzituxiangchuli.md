# 数字图像处理笔记（一至三章）

## 一、数字图像基础

+ 数字图像的概念（理解）

	1. 什么是数字图像

		答：数字图像用二维的亮度函数$f(x,y)$

		​		—x和y表示空间坐标，

		​		—$f(x,y)$是关于$(x,y)$的函数值，与成像与该点的光强成正比。

- 数字图像的多样性（理解）

	   1.    成像类型

		==反射成像==是由物体表面反射的电磁波到达成像传感器而成像

		==发光成像==是物体本身辐射的电磁波到达成像传感器而成像

		==吸收成像==是由于一部分透过物体的电磁波到达成像传感器而成像

	2. 尺度概念

		日常观察物体或场景1~10^3^

		天文望远镜 > 10^30^

		电子显微镜 < 10^-10^

- 图像数据量（理解）

	   1.    依据图像的数据量，将图像主要分为4类：

		+ 二值图像

			每一个像素仅占用1位，灰度值为0或1，其中，0表示黑色，1表示白色，

		+ 灰度图像

			黑与白之间有多级灰色深度。常用的成像和显示设备中，灰度图像中每一个像素的灰度级通常采用8 位表示，具有2^8^=256个灰度级，每一个灰度像素占用1个字节。
		
		- 索引图象
		
			包含颜色查找表，通过查找映射的方法表示彩色图像的颜色。颜色查找表通常用一个维数为$n×3$ 的数组来表示，$n$为索引图像的像素值总数，每一行的3维向量为R、G、B颜色分量。
		
		- 真彩色图像
		
			每一个彩色像素用一个3维向量来表示，由R、G、B颜色分量组成。R、G、B颜色分量各占用8 位表示相应颜色分量的亮度，每一个颜色分量各有256个灰度级，这3个字节组合可以产生2^24 = 1677万种不同的颜色。

- 图像数字化（掌握）

	1. 数字图像、采样、量化

		自然场景的空间位置和辐射度都是连续量，将连续（模拟）信号转变为离散（数字）信号的转换过程。

		**数字图像**—空间坐标和亮度上都离散化的图像

		图像数字化的两个步骤：==采样==—空间坐标$(x,y)$的离散化；==量化==—亮度值$f(x,y)$的离散化

		+ 采样

			图像在空间上的离散化，空间上连续的图像转换成离散点（像素，PIXEL） ==正方形点阵结构==对水平方向和垂直方向的空间位置进行光栅化；

			==正六边形采样网格==更符合人眼的视觉特性。 视网膜上分布的视神经细胞的排列方式为正六边形结构，且这些细胞在水平方向和垂直方向上相比在对角方向上对高频信号有更高的光敏感度。

			==采样决定空间分辨率==，反映图像数字化的像素密度，以及图像的有效像素。

			![image-20251104145300289](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104145300289.png)![image-20251104145333702](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104145333702.png)

		- 量化

			图像函数值（灰度值）的离散化（取值的数字化）

			常见的像素的灰度级数2^8^=256，灰度级位数越大，量化误差越小。

			==量化决定灰度级分辨率==，指可分辨的最小灰阶变化。

	2. 空间分辨率、灰度级分辨率及两者与图像质量的关系

		![image-20251104145612162](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104145612162.png)![image-20251104145623639](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104145623639.png)

- 数字图像的常见失真类型（理解）

	- 混叠效应

		当采样间隔过大时，由于采样频率小于奈奎斯特频率，而引起频谱的高频成分发生相互交叠，频谱重叠导致失真。

	- 模糊效应

		长时间曝光过程中成像设备与成像场景的相对运动而造成==运动模糊==；

		光学镜头散焦、大气湍流、短时间曝光而造成==散焦模糊==。

	- 块效应

		在相邻块之间引入的非连续性，视觉上看似马赛克的效果。 

		块效应产生的原因： 对图像分块处理而引起的块状失真。 在图像压缩编码中，分块离散余弦变换编码和分块运动补偿是造成块效应的两个主要原因。

	- 噪声

		感光器件接收光信号并输出的过程中产生的异常像素。 根据噪声的分布和统计特征，成像系统的噪声源可以分为光电散粒噪声、读出噪声和脉冲噪声等。

## 二、数字图像处理基础

- 数字图像处理概况（了解）

	1. 图像处理的关键阶段

		根据语义从低级到高级，广义图像处理分为三个层次：==图像处理==、==图像分析==和==图像理解==。

		![image-20251104150927260](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104150927260.png)

- 像素空间关系（理解）

  1.   领域（4-、对角-、8-；邻接、连通、区域与边界）

	- 邻接性

		像素$p$和$q$邻接的两个必要条件：两个像素的位置在某种情况下是否相邻； 两个像素的值是否满足某种相似性

		![image-20251104151420518](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104151420518.png)![image-20251104151428207](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104151428207.png)

	- 连通性

		若从像素$q$到像素$p$之间存在一条一序列像素组成的折线，且该折线上的像素灰度值均满足上述特定相似性准则，则称这一序列像素组成了一条从像素$q$到像素$p$的通路。设$q$和$p$是某一图像子集$S$中的两个像素，若存在一条完全由$S$中的像素组成的从$q$到$p$的通路，则称像素$q$和$[]$是连通的。

		在4邻接下定义的通路称为==4连通==

		在8邻接下定义的通路称为==8连通==

		![image-20251104151927331](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104151927331.png)![image-20251104151938267](../../Users/86137/AppData/Roaming/Typora/typora-user-images/image-20251104151938267.png)

		区域的==内部==和==边界==必须采用不同的连通性来定义，否则会出现==歧义性==。

	2. 距离度量

		像素在空间上的近邻程度可以用像素之间的距离来度量。

		**正定性：两点之间距离大于等于0**

		**对称性：距离与方向无关**

		**三角不等式：两点之间直线距离最短**

		==范数是一个从向量空间到数域的函==，是为向量空间中的非零向量赋予正长度。 

		使用范数可以度量两个向量之间的距离。

		==街区距离==形成一个==菱形==、==棋盘距离==形成一个==正方形==

