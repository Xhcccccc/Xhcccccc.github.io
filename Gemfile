source "https://rubygems.org"

# 只保留一个 jekyll 定义
gem "jekyll", "~> 4.4.1"

# 锁定 sass-embedded 到兼容版本
gem "sass-embedded", "~> 1.74.0"

# 或者使用传统的 sassc（二选一）
# gem "jekyll-sass-converter", "~> 2.0"
# gem "sassc", "~> 2.4"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
  # 添加您的其他插件
end

# Windows 兼容性
gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]

# 主题（如果有的话）
# gem "minima", "~> 2.5"
